import Header from "../components/Header";
import ParkingSlots from "../components/ParkingSlots";
import AllocatedSlots from "../components/AllocatedSlots";

const Home = () => {
  return (
    <>
      <Header />
      <ParkingSlots />
      <AllocatedSlots />
    </>
  );
};

export default Home;
